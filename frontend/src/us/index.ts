// US region frontend sibling — entry barrel.
// See README.md for structure + mounting rules.

// Pages (empty — to be implemented in a follow-up phase):
// export { default as USDashboard } from './pages/Dashboard'
// export { default as USOrderBook } from './pages/OrderBook'

// Region marker for the router:
export const REGION_CODE = 'us' as const
