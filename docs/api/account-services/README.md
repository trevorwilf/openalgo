# This section moved

The `/api/v1` lane endpoint references now live at
[`docs/api/v1/account-services/`](../v1/account-services/).

`/api/v1` is the legacy India-only lane per
[ADR 0005](../../adr/0005-two-lanes-legacy-and-promoted.md). The
top-level [`docs/api/README.md`](../README.md) carries the
multi-lane top nav.
